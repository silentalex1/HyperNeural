services:
  - type: web
    name: inferforge-api
    runtime: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt && pip install .
    startCommand: python -m uvicorn inferforge.server.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
